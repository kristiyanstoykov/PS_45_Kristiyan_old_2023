<Window x:Class="UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        xmlns:view="clr-namespace:UI.Components"
        xmlns:view2="clr-namespace:UI.View.Components"
        mc:Ignorable="d"
        Title="List of all windows" WindowStartupLocation="CenterScreen" Height="800" Width="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <view:StudentsList x:Name="studentsList" DataContext="{Binding UsersPresenterVM, Source={StaticResource Locator}}" Padding="2" Grid.Row="0" />
        <view:DatabaseLoggerList x:Name="databaseLoggerList" DataContext="{Binding LoggerPresenterVM, Source={StaticResource Locator}}" Padding="2" Grid.Row="1" />
        <view2:AddUser x:Name="AddUser" DataContext="{Binding UsersPresenterVM, Source={StaticResource Locator}}" Grid.Row="2"/>
    </Grid>
</Window>
